# 가능성 예보국 초등학생 브라우저 실사용 재점검 계획

## 목적

브라우저에서 처음 사용하는 초등학생이 시작부터 미션 완료까지 스스로 진행할 수 있는지 다시 확인하고, 막히는 문장·버튼·안내를 고친다.

## 점검 흐름

`첫 화면 → 활동 방법 → 개념 안내 → 안내 연습 → 첫 자료 세기 → 첫 예보 → 첫 선택 → 새 자료 합치기 → 이유 고르기 → 처음과 나중 비교 → 미션 5개`

## 학생 관점 점검 질문

- 지금 무엇을 해야 하는지 한 문장만 읽고 알 수 있는가?
- 버튼을 누르기 전에 필요한 자료가 화면에 보이는가?
- 틀렸을 때 무엇을 다시 확인해야 하는지 구체적으로 알 수 있는가?
- `예보`, `분수`, `자료` 같은 낱말이 화면 예시와 연결되어 있는가?
- 다음 단계로 가는 버튼이 화면 아래에 숨거나 같은 이름으로 헷갈리지 않는가?
- 모바일 화면에서도 카드·선택 상자·피드백 문장이 잘리지 않는가?

## 수정 기준

- 중요한 행동 버튼은 학생이 찾기 쉬운 위치와 상태를 유지한다.
- 한 화면의 핵심 안내는 먼저 해야 할 행동을 앞에 쓴다.
- 어려운 낱말에는 바로 다음에 실제 예시를 붙인다.
- 오류 안내는 문제 이름과 다시 볼 위치를 함께 알려 준다.
- 수정 뒤 기존 수학 자료·정답 계약은 바꾸지 않는다.
- 업데이트 내역에 2026-08-25 재점검·개선 내용을 기록한다.

## 수용 기준

- 375×812와 데스크톱 화면에서 시작부터 요약까지 핵심 행동을 찾을 수 있다.
- 안내 연습과 미션 5개 전체를 브라우저에서 완료할 수 있다.
- 어려운 문장이나 다음 행동이 모호한 화면을 발견하면 수정 후 회귀 테스트를 추가한다.
- 가로 넘침, 잘린 피드백, 중복 진행 이름이 없다.
- 타입 검사·린트·콘텐츠 검사·단위 테스트·빌드·E2E가 통과한다.
- 구현 결과와 남은 실제 학생 관찰 필요 사항을 이 문서에 기록한다.

## 구현 기록

- 상태: 구현·로컬/공개 브라우저 재검증·GitHub Pages 공개 배포 완료
- 실제 아동 참가자를 모집한 조사가 아니라, 초등학생의 첫 사용 상황을 가정한 브라우저 직접 조작 점검이다.

## 2026-08-15 복구 점검 결과와 해결

- 작업 폴더에는 이 계획 파일만 남아 있고 `.git`, `app/`, `tests/`, `package.json`, `.openai/hosting.json`이 없다.
- 공개 주소 `https://possibility-forecast-office.wbmaker.chatgpt.site`는 HTTP 200으로 응답한다.
- 현재 Sites 커넥터에서는 이전 프로젝트 ID를 찾지 못해 원본 저장소를 다시 받을 수 없다.
- 이전에 사용한 Playwright 의존성도 현재 작업 폴더에 없어 브라우저 직접 자동화가 실행되지 않았다.
- 따라서 이번 단계에서는 실제 수정·테스트·커밋·재배포를 진행하지 않았다.
- 2026-08-25에 원본 저장소를 외장 드라이브 작업 경로에서 복구했고, `main`이 원격 브랜치와 일치함을 확인했다.

## 2026-08-25 재개 실행 계획

1. 현재 배포본과 로컬 원본의 첫 화면부터 미션 완료까지를 데스크톱과 375×812 화면에서 직접 조작한다.
2. 학생이 멈출 수 있는 화면마다 `지금 할 일`, 필요한 자료, 틀린 뒤 다시 볼 곳, 다음 버튼의 발견 가능성을 기록한다.
3. 발견 결과를 이 문서와 QA 보고서에 먼저 적고, 어려운 문장·안내·버튼 강조를 작은 단위로 수정한다.
4. 중요한 다음 행동 버튼에는 움직임 줄이기 설정을 존중하는 `gi-pulse` 계열 아우라 효과를 적용한다.
5. 단위 테스트·타입 검사·린트·콘텐츠 검사·빌드·E2E와 브라우저 회귀 점검을 실행한다.
6. 업데이트 내역과 공개 배포본을 갱신하고 실제 공개 주소에서 최종 흐름을 다시 확인한다.

## 학생 진행 흐름 시각화

```mermaid
flowchart LR
  A[시작] --> B[개념과 방법]
  B --> C[첫 자료 세기]
  C --> D[첫 예보]
  D --> E[선택]
  E --> F[새 자료 합치기]
  F --> G[이유와 비교]
  G --> H[미션 완료]
```

각 화면은 학생이 다음 질문에 바로 답할 수 있어야 한다: **“지금 무엇을 보고, 무엇을 누르면 되지?”**

## 2026-08-25 브라우저 기준선 결과

실제 아동 참가자를 모집한 조사가 아니라, 처음 방문한 초등 5~6학년 학생을 가정해 화면에 보이는 안내만으로 안내 연습과 미션 5개를 직접 끝까지 조작했다.

### 우선순위가 높은 막힘

1. 공개 주소가 `404 찾을 수 없음`으로 열려 활동을 시작할 수 없다.
2. 미션 2의 선택지와 최종 요약에 `more-likely`, `less-likely`가 그대로 보여 뜻을 알 수 없다.
3. 첫 선택 화면에는 문제를 푸는 데 필요한 칸 수·횟수·비율이 보이지 않아 앞 화면 값을 외워야 한다.
4. 모두 합친 횟수 화면에는 `첫 자료 + 새 자료`의 중간값이 없어 기억과 암산에 의존한다.
5. 신호판의 칸으로 예보하는 미션에서 방금 센 실제 결과와 예보에 사용할 구조 비율의 차이가 바로 설명되지 않는다.

### 추가 개선점

- 같은 결과 그림이 찾을 항목 수만큼 반복되어 모바일 스크롤이 길고, 어느 그림을 세는지 헷갈릴 수 있다.
- 고르지 않았을 때와 틀렸을 때 같은 오류가 나와 다음 행동을 구체적으로 알기 어렵다.
- `파란 표식가 나타남`, `빨강가 나타남`, `그대로예요.:`처럼 조사·문장부호 오류가 있다.
- 다음 행동 버튼이 다른 버튼과 비슷해 한눈에 찾기 어렵고, 모바일에서 행동 순서를 알려 주는 짧은 안내가 부족하다.

### 구현 결정

- 모든 단계 위쪽에 `지금 할 일` 한 문장을 제공한다.
- 첫 선택과 최종 선택에는 판단에 필요한 자료 요약을 같은 화면에 다시 보여 준다.
- 모두 합친 횟수에는 항목마다 `첫 자료 a번 + 새 자료 b번 = ?` 도움식을 보여 준다.
- 실제 결과와 신호판 칸을 함께 쓰는 미션에는 `이번에 나온 결과`와 `예보에 쓸 분수`를 나란히 설명한다.
- 내부 영어 값은 쉬운 한국어 표시 이름으로 바꾸고 최종 요약에도 같은 이름을 사용한다.
- 미선택·오답 안내를 나누고, 중요한 다음 버튼에 `gi-pulse` 아우라와 모바일 전체 너비를 적용하되 움직임 줄이기 설정을 존중한다.
- 조사 선택은 받침 여부를 처리해 문장을 자연스럽게 만든다.

## 2026-08-25 구현 및 재검증 결과

### 구현 완료

- 내부 영어 선택값을 쉬운 한국어로 통일했다.
- 같은 실행 결과는 한 번만 보여 주고 '함께 세는 결과'라고 알려 모바일 스크롤을 줄였다.
- 모두 합친 횟수마다 '첫 자료 + 새 자료 = ?' 도움식을 제공했다.
- 선택 화면에서 바로 비교할 비율을 다시 보여 주어 앞 화면 값을 외울 필요를 없앴다.
- 신호판 미션에서 '이번에 나온 결과'와 '예보에 쓸 신호판의 칸'을 나란히 설명했다.
- 빈칸, 분수 위·아래 뒤집기, 일반 오답, 선택 누락, 같은 비율 오답의 안내를 각각 다르게 제공했다.
- 모든 풀이 단계에 '지금 할 일'을 붙이고, 중요 버튼에 움직임 줄이기를 존중하는 gi-pulse-aura 효과를 적용했다.
- 모바일 시작 버튼과 단계 버튼을 한 줄 전체 폭으로 넓히고 분수 입력을 '분수 위', '분수 아래'로 표시했다.
- 업데이트 내역에 2026-08-25 학생 실사용 재점검 내용을 기록했다.
- Next.js를 16.3.2로 갱신해 배포 런타임 보안 감사 결과를 취약점 0건으로 만들었다.

### 실제 브라우저 재검증

- 375×812에서 안내 연습과 미션 1~5를 직접 완료했고 최종 요약 5행을 확인했다.
- 모바일 시작 버튼은 292px, 단계 중요 버튼은 328px로 표시됐다.
- 첫 자료 세기에서 '분수 위 · 나온 횟수', '분수 아래 · 전체 횟수'가 읽혔다.
- 빈 선택 안내, 미션 2의 8/10 비교값, 누적 8 + 1, 같은 비율의 '자료를 더 보기', 미션 5의 5/5와 1/2 비교를 확인했다.
- 375px 화면의 가로 넘침과 현재 공개 페이지 콘솔 오류는 모두 없었다.
- 실제 아동 참가자를 모집한 조사는 아니며, 초등 5~6학년 첫 사용 상황을 가정한 직접 조작 휴리스틱 테스트다.

### 자동 검증

- 타입 검사, 린트, 콘텐츠 검사 통과
- 단위 테스트 43개 통과
- 서버 렌더링 검사 2개 통과
- 브라우저 E2E 13개 통과
- Sites용 Vinext 빌드와 GitHub Pages 정적 빌드 통과
- 배포 런타임 보안 감사 취약점 0건

### 공개 배포

- 공개 주소: https://wbmaker2.github.io/possibility-forecast-office/
- 기존 Sites 프로젝트는 현재 계정에서 찾을 수 없어 중복 프로젝트를 만들지 않고, 저장소에 이미 구성된 GitHub Pages 워크플로로 공개 주소를 복구했다.
- GitHub Pages 배포 실행: https://github.com/WBmaker2/possibility-forecast-office/actions/runs/32789496782
