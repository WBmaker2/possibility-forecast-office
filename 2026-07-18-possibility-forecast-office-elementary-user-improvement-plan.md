# 가능성 예보국 초등학생 실사용 개선 계획

작성일: 2026-07-18  
대상: `possibility-forecast-office`  
실사용 흐름: 시작 → 개념 안내 → 안내 연습 → 첫 미션 → 오류·뒤로가기 → 최종 요약

## 1. 테스트 관점

초등학생이 교사의 추가 설명 없이 화면만 보고 활동한다고 가정했습니다.

- 한 번 읽고 지금 무엇을 해야 하는지 알 수 있는가
- 수학에서 꼭 필요한 말과 화면 운영을 위한 어려운 말이 구분되는가
- 틀렸을 때 무엇을 다시 보면 되는지 구체적으로 알려 주는가
- 모바일 첫 화면에서 시작 행동을 바로 찾을 수 있는가
- 첫 자료와 새 자료, 신호판의 칸과 실제로 나온 결과의 차이를 혼동하지 않는가

## 2. 실사용에서 발견한 문제

| 우선순위 | 발견 사항 | 학생에게 생기는 어려움 | 실제 확인 근거 |
|---|---|---|---|
| P0 | 375×812에서 `예보 시작` 버튼의 위쪽 위치가 약 1,169px임 | 첫 화면에서 시작 버튼이 보이지 않아 긴 설명을 모두 내려야 함 | 모바일 브라우저 실측 |
| P0 | `목표 사건`, `결과 배열`, `알려진 구조`, `관찰 비율`, `판단 규칙`, `임시 선택`, `근거 연결`, `전후 기록`이 핵심 흐름에 반복됨 | 문장의 뜻보다 낱말 해석에 힘을 쓰게 됨 | 개념·조건·횟수·예보·선택·기록 화면 직접 사용 |
| P0 | 횟수 오류 안내가 `목표 사건 횟수와 전체 횟수`를 다시 세라고 함 | 무엇을 세어야 하는지 구체적인 색·결과가 보이지 않음 | 빈 선택 뒤 오류 배너 확인 |
| P1 | 안내 연습에서 신호판의 칸 `4/8`과 실제로 나온 결과 `4/6`을 한 화면에 보여 주지만 `장치 구조`, `출처`로 설명함 | 두 분수가 왜 다른지보다 전문 용어가 먼저 보임 | 첫 예보 화면 직접 사용 |
| P1 | 개념 화면 진행 표시가 `개념 안내 · 개념 안내 · 전체 0/6`으로 중복됨 | 진행 정보가 기계적으로 보이고 읽을 정보가 늘어남 | 개념 화면 DOM 확인 |
| P1 | 시작 화면의 `이 탭의 메모리`, `가상의 반복 실험` 문장이 기술적임 | 저장 여부와 활동 성격을 바로 이해하기 어려움 | 시작 화면 직접 확인 |
| P1 | `교사용` 버튼이 학생용 주요 메뉴와 같은 무게로 보임 | 학생이 자신의 다음 행동으로 오해할 수 있음 | 데스크톱·모바일 시작 화면 확인 |
| P2 | 개념 오답 안내가 `자료에서 보인 횟수만으로 불가능이라고 할 수 없어요`처럼 길고 추상적임 | 무엇이 틀렸고 어떤 말로 바꿔야 하는지 한 번에 파악하기 어려움 | `0/10 → 불가능` 선택 뒤 피드백 확인 |

### 디자인 기준선

- 첫인상: 차분하고 정돈됐지만 초등학생용 활동보다 수학 보고서에 가까움
- 시선 순서: 앱 제목 → 진행 표시 → 긴 설명 → 학습 경로, 시작 버튼은 모바일 첫 화면 밖
- 디자인 점수: B
- 학생 친화성 점수: C+
- 유지할 강점: 18px 모바일 글자, 44px 이상 터치 영역, 가로 넘침 0px, 점수·경쟁·학생 정보 저장 없음

## 3. 구현 범위

### A. 쉬운 말로 바꾸기

- `목표 사건` → `찾을 결과`
- `결과 배열` → `결과 그림`
- `알려진 구조` → `신호판의 칸`
- `장치에서 계산` → `신호판의 칸으로 계산`
- `자료에서 관찰` → `실제로 나온 결과로 계산`
- `임시 선택` → `첫 선택`
- `판단 규칙` → `고르는 기준`
- `근거 연결` → `이유 고르기`
- `전후 기록` → `처음과 나중 비교`
- `관찰 비율` → `나온 결과의 비율`
- `누적 자료` → `모두 합친 자료`

수학 핵심어인 `가능성`, `분수`, `예보`는 유지하되 바로 옆 문장을 구체적으로 씁니다.

### B. 지금 할 일 강조

- 조건·횟수·예보·선택·이유 화면의 안내를 짧은 한 문장으로 정리합니다.
- 오류 안내에는 실제로 다시 세어야 할 결과 이름을 넣습니다.
- `0/10`, `10/10` 오개념은 `10번 중 0번`, `10번 중 10번`의 뜻을 함께 설명합니다.
- 신호판의 칸과 실제로 나온 결과의 분수가 다를 수 있음을 두 문장으로 나누어 설명합니다.

### C. 모바일 시작 행동 개선

- `예보 시작`, `개념 먼저 보기`를 소개 문장 바로 아래로 옮깁니다.
- 학습 순서와 시간·미션·저장 정보는 시작 버튼 아래의 참고 정보로 둡니다.
- 375×812 첫 화면에서 `예보 시작` 버튼이 보이거나 한 번의 짧은 스크롤 안에 나타나게 합니다.

### D. 진행·메뉴·기록 정리

- 같은 진행 이름이 연속으로 중복되지 않게 합니다.
- `교사용`을 `선생님 안내`로 명확히 바꾸고 학생의 시작 버튼보다 시각적 우선순위를 낮춥니다.
- 기록 화면 제목과 안내를 `내 예보 돌아보기`, `처음과 나중 비교`처럼 바꿉니다.
- 2026-07-18 학생 실사용 개선 내용을 앱의 업데이트 내역에 추가합니다.

## 4. 예상 변경 파일

- `app/forecast/ForecastOffice.tsx`: 시작 화면 순서, 진행 문구, 오류·성공 안내, 개념 문장
- `app/forecast/MissionFlow.tsx`: 단계 제목·버튼·안내 문장, 업데이트 내역
- `app/forecast/visuals.tsx`: 조건·출처·규칙·자료 설명 문구
- `app/forecast/session.ts`: 학생용 단계 이름
- `app/globals.css`: 모바일 시작 행동, 안내 강조, 메뉴 위계
- `tests/e2e/forecast-office.spec.ts`: 쉬운 문구와 모바일 시작 버튼 회귀 검사
- `docs/verification-log.md`: 실제 학생 관점 브라우저 검증 기록

## 5. 수용 기준

- 375×812에서 `예보 시작` 버튼의 위쪽 위치가 700px 이내입니다.
- 학생 핵심 흐름에 `목표 사건`, `결과 배열`, `알려진 구조`, `판단 규칙`, `임시 선택`, `근거 연결`, `전후 기록`이 표시되지 않습니다.
- 횟수 오류 안내가 찾을 결과를 구체적으로 알려 줍니다.
- 신호판의 칸과 실제로 나온 결과의 차이를 초등학생용 두 문장으로 설명합니다.
- 개념 화면 진행 문구에 같은 말이 두 번 연속 나오지 않습니다.
- 데스크톱과 375px에서 가로 넘침이 없습니다.
- 기존 안내 연습 1개와 미션 5개 전체 완주가 유지됩니다.
- typecheck, lint, 단위 테스트, 콘텐츠 검사, build, SSR, Playwright E2E가 모두 통과합니다.
- 변경된 코드 파일은 각각 500줄 미만을 유지합니다.

## 6. 범위 밖

- 수학 자료 배열·정답·분수·판단 계약 변경
- 로그인·영구 저장·점수·순위 추가
- 외부 API·이미지 생성·새 미션 추가
- GitHub 푸시·Sites 재배포·HVC 등록

## 7. 구현 기록

- 상태: 구현·회귀 검사 완료
- 계획 승인: 사용자가 `먼저 md 문서로 기록을 남긴 뒤 구현까지 완료`하도록 명시해 별도의 승인 대기를 생략함
- 변경 파일: `app/content/tutorial.ts`, `app/content/missions.ts`, `app/forecast/ForecastOffice.tsx`, `app/forecast/MissionFlow.tsx`, `app/forecast/visuals.tsx`, `app/forecast/session.ts`, `app/globals.css`, `tests/e2e/forecast-office.spec.ts`, `docs/verification-log.md`
- 구현 내용: 시작 버튼을 소개 바로 아래로 옮기고, 375×812에서 위쪽 위치가 700px 이내임을 자동 검사했습니다. 핵심 화면의 어려운 운영 용어를 `찾을 결과`, `결과 그림`, `신호판의 칸`, `모두 합친 자료`, `첫 선택`, `이유 고르기`, `처음과 나중 비교`로 바꿨습니다. 한 결과·여러 결과 미션의 횟수 오류도 다시 세어 볼 결과를 구체적으로 안내합니다.
- 검증 결과: typecheck·lint·단위 30개·콘텐츠 검사·build·SSR 2개·Playwright E2E 12개 통과. Codex 앱 내 브라우저에서 1280×900과 375×812 화면을 직접 조작해 시작·개념·오류 안내·최종 요약을 확인했습니다.
- 실사용 수치: 375×812의 `예보 시작` 버튼 위쪽 위치가 약 1,169px에서 520px로 줄었고, 데스크톱·모바일 가로 넘침은 0px을 유지했습니다. 개념·요약 화면의 어려운 핵심 용어와 진행 이름 중복도 제거했습니다.
- 독립 재검토: 문구·수학 규칙·모바일 피드백 레이아웃을 별도 검토했으며 Critical·Important·Minor 잔여 발견 없이 승인했습니다.
