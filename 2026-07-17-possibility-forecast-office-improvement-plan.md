# 가능성 예보국 기능·디자인·UI 개선 계획

작성일: 2026-07-17  
대상: `possibility-forecast-office` MVP  
기준 흐름: 시작 화면 → 개념 안내 → 첫 자료 세기 → 누적 자료 예보 → 최종 기록

## 1. 분석 요약

현재 앱은 알려진 장치 구조의 가능성과 관찰 자료의 비율을 구분하고, 안내 활동 1개와 고정 미션 5개를 끝까지 수행하는 핵심 학습 계약을 충족합니다. 320px 가로 넘침과 중대 접근성 위반도 기존 자동 검사에서 발견되지 않았습니다.

다만 실제 화면과 상호작용을 다시 확인한 결과, 학습을 방해할 수 있는 기능·UI 문제가 남아 있습니다.

| 우선순위 | 발견 사항 | 학생에게 미치는 영향 | 근거 |
|---|---|---|---|
| P0 | 잘못된 선택의 안내가 화면에 표시되지 않고 스크린 리더용 영역에만 존재함 | 화면을 보는 학생은 왜 다음 단계로 넘어가지 않는지 알기 어려움 | `notice`가 `sr-only`에만 렌더링됨 |
| P0 | 횟수 입력이 분모 크기에 따라 라디오 2n+1개로 증가함 | 누적 분모 40인 자료에서는 한 스트림에 81개 선택지가 보여 탐색 부담이 큼 | 현재 `CountPicker`가 0..n, 1..n 라디오를 모두 렌더링함 |
| P1 | 안내 연습 중 진행 막대가 0%에 머물고 현재 단계만 표시함 | 전체 6개 활동 중 어디에 있는지, 얼마나 남았는지 알기 어려움 | 진행률 계산이 일반 미션에서만 동작함 |
| P1 | 단계 이동 뒤 제목에 브라우저 기본 주황색 테두리가 크게 남음 | 현재 위치 안내보다 오류 표시처럼 보이고 시각적 완성도를 낮춤 | `tabIndex=-1` 제목에 프로그램 포커스 후 기본 outline 노출 |
| P1 | 상단 도움말 버튼 높이가 38px임 | 권장 44px 터치 영역보다 작아 모바일 조작성이 떨어짐 | 1280px 렌더링 치수 검사 |
| P2 | 시작 화면이 기능 설명 뒤 바로 버튼으로 끝남 | ‘첫 자료 → 새 자료 → 다시 예보’ 학습 구조를 시작 전에 예측하기 어려움 | 실제 첫 화면 관찰 |
| P2 | 다섯 번째 개념 카드가 2열 그리드에서 홀로 남음 | 정보 위계가 끊기고 화면이 덜 의도적으로 보임 | 실제 개념 화면 관찰 |

### 디자인 기준선

- 첫인상: 목적은 명확하지만 ‘학습 도구’보다 정적인 행정 화면에 가까운 인상
- 시선 순서: 큰 제목 → 3개 정보 상자 → 시작 버튼
- 디자인 점수: B-
- AI 템플릿 인상 점수: B
- 강점: 차분한 남색·청록 팔레트, 충분한 본문 글자 크기, 명확한 기본 버튼 대비
- 유지할 원칙: 점수·경쟁 없음, 학생 정보 비수집, 드래그 없음, 고정 자료, 수학 출처 구분

## 2. 구현 범위

### A. 화면에 보이는 학습 피드백

- 내부 오류 코드와 학생용 문장을 분리합니다.
- 오류·성공·이전 단계 안내를 화면 상단 피드백 배너에 표시합니다.
- 오류 발생 시 배너로 포커스를 옮겨 원인과 다음 행동을 바로 읽을 수 있게 합니다.
- `source-overclaim`, `count-mismatch` 같은 내부 영문 코드는 학생 화면에 노출하지 않습니다.

### B. 횟수 입력 단순화

- 스트림마다 ‘나온 횟수’와 ‘전체 횟수’ 두 개의 선택 상자를 제공합니다.
- 선택 결과를 분수 모양으로 묶어 분자와 분모의 역할을 시각적으로 연결합니다.
- 기존 정답 판정과 데이터 구조는 유지합니다.
- 누적 분모 40에서도 화면에 노출되는 주 선택 컨트롤은 스트림당 2개로 제한합니다.

### C. 전체 여정 진행률

- 안내 연습 1개와 일반 미션 5개를 합친 6개 활동을 진행 단위로 사용합니다.
- ‘안내 연습/미션 n’, 현재 단계, ‘전체 n/6’을 함께 표시합니다.
- 안내 연습에서도 단계 진행에 따라 막대가 증가하고 최종 요약에서 100%가 되게 합니다.

### D. 시각·터치·시작 안내 개선

- 프로그램이 이동시킨 제목에는 불필요한 기본 outline을 숨기되 실제 버튼·입력의 `:focus-visible`은 유지합니다.
- 도움말 버튼의 최소 높이를 44px로 맞춥니다.
- 제목 굵기와 줄바꿈을 정리해 화면 위계를 강화합니다.
- 시작 화면에 ‘첫 자료 세기 → 새 자료 합치기 → 예보 다시 보기’ 학습 경로를 추가합니다.
- 개념 카드 다섯 번째 항목을 전체 너비로 배치해 2열 그리드의 빈칸을 제거합니다.

### E. 기록과 업데이트 내역

- 앱의 ‘업데이트 내역’에 2026-07-17 기능·UI 개선 내용을 추가합니다.
- 이 문서의 구현 기록과 검증 결과를 실제 완료 상태로 갱신합니다.
- `docs/verification-log.md`에 새 회귀 검사 결과를 추가합니다.

## 3. 변경 예상 파일

- `app/forecast/ForecastOffice.tsx`: 진행률, 피드백 상태, 시작 학습 경로
- `app/forecast/MissionFlow.tsx`: 횟수 선택 UI, 학생용 문구, 업데이트 내역
- `app/forecast/progress.ts`: 전체 6개 활동 진행률 순수 계산
- `app/globals.css`: 피드백, 진행률, 선택 상자, 터치 영역, 제목·개념 카드 스타일
- `tests/unit/forecast-session.test.ts`: 진행률 계산 회귀 검사
- `tests/e2e/forecast-office.spec.ts`: 보이는 오류 안내, 선택 상자, 전체 흐름 회귀 검사
- `docs/verification-log.md`: 실제 검증 결과

## 4. 수용 기준

- 잘못된 횟수를 선택하면 학생이 볼 수 있는 구체적 안내가 표시됩니다.
- 학생 화면에 내부 영문 오류 코드가 나타나지 않습니다.
- 모든 횟수 단계에서 스트림당 두 개의 선택 상자로 답할 수 있습니다.
- 안내 연습에서 진행률이 0%에 고정되지 않고, 최종 요약에서 전체 6/6이 표시됩니다.
- 모든 상단 도움말 버튼의 높이가 44px 이상입니다.
- 단계 제목의 프로그램 포커스가 접근성 흐름은 유지하면서 큰 기본 테두리를 만들지 않습니다.
- 320px에서 가로 넘침이 없고 시작·개념·횟수·요약 화면을 사용할 수 있습니다.
- 기존 안내 활동 1개와 일반 미션 5개 전체 완주 E2E가 통과합니다.
- typecheck, lint, 단위 테스트, 콘텐츠 검증, build, SSR, axe 검사가 모두 통과합니다.
- 변경된 코드 파일은 각각 500줄 미만을 유지합니다.

## 5. 범위 밖

- 수학 자료 배열, 정답, 판단 규칙 변경
- 로그인, 저장, 분석 도구, 외부 API 추가
- 점수, 순위, 시간 측정 추가
- HVC 등록·갤러리 동기화
- Sites 공개 범위 변경

## 6. 구현 기록

### 2026-07-17 구현 완료

- 상태: 구현·회귀 검사 완료
- 실행 승인: 사용자가 분석 후 개선·구현까지 마무리하도록 위임한 요청을 이 범위의 실행 승인으로 해석했으며, 계획 변경 없음
- 변경 파일: `app/forecast/progress.ts`, `app/forecast/ForecastOffice.tsx`, `app/forecast/MissionFlow.tsx`, `app/globals.css`, `tests/unit/forecast-session.test.ts`, `tests/e2e/forecast-office.spec.ts`, `playwright.config.ts`, `docs/verification-log.md`
- 결정 사항: 수학 자료·정답·판단 규칙·저장 정책은 바꾸지 않았습니다. 화면의 내부 오류 식별자는 학생용 한국어 피드백 배너로 바꾸고, 오류 배너에 포커스를 옮겼습니다. 횟수 입력은 스트림마다 `나온 횟수`와 `전체 횟수` 선택 상자 두 개로 줄였습니다.
- 진행률: 안내 연습 1개와 미션 5개를 총 6활동으로 계산하는 순수 helper와 단위 검사를 추가했습니다. 안내 연습 중에도 단계에 따라 진행 막대가 증가하며, 최종 요약은 6/6·100%를 표시합니다.
- 안정화 보완: `0/5`의 0을 고른 뒤 선택을 비우면 불완전한 답으로 처리하도록 입력값을 보존·검사했습니다. 예보·첫 선택·최종 선택·근거 기록 성공 뒤에는 다음 할 일을 알려 주는 성공 배너를 표시합니다.
- 검증 결과: typecheck·lint·단위 30개·콘텐츠 검사·build·SSR 2개·Playwright E2E 10개 통과. Codex 앱 내 브라우저에서도 1280px와 320px로 시작·개념·횟수·요약 화면을 직접 확인했으며 가로 넘침은 0px, 상단 도움말 버튼은 모두 44px, 학생 화면의 내부 오류 코드 노출은 0건이었습니다.
- 최종 디자인 평가: B-에서 B+로 개선. 시작 화면의 학습 여정, 의도적인 개념 카드 배치, 보이는 피드백과 단순한 횟수 선택으로 정적 행정 화면 인상을 줄였습니다.
- 독립 재리뷰: `0/5` 빈 선택, 성공 안내 문구·검사 범위를 보완한 뒤 Critical·Important·Minor 발견 사항 0건으로 승인받았습니다.
